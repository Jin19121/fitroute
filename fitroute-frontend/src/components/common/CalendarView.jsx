// src/components/common/CalendarView.jsx
// TODO: 리포트 페이지에서 사용 예정

export default function CalendarView({ type, data, year, month, onMonthChange, onSelectDate }) {
    return (
        <div className="flex-1 flex items-center justify-center text-gray-400 text-sm">
            캘린더 (준비 중)
        </div>
    );
}